# Meeting Report

**Date:** 2026-04-07
**Meeting with:** Professor
**Project:** Web-Project

## 1. Participants
- 

## 2. Meeting Summary
- Professor suggested folders should show documents only when the folder is clicked.
- Each folder should still have a name, id, user_id, and a reference to the parent/root folder.
- Folder visualization should use a loop with a folder icon for display.
- Add file icons based on file extension; fallback to a generic file icon when no extension-specific icon exists.
- Folder query should retrieve folders where `user_id` equals the current user id.
- Sharing logic should include both documents owned by the user and documents shared with the user via the share table.
- Confirm using `checkquery` library or equivalent for adding new folders.

## 3. Decisions Made
- Folder show documents only when clicked, no recursive search.
- Ensure folder metadata includes name, id, user_id, and parent folder tracking.
- Add file icons depending on file extension, with a default icon for unknown extensions.
- Use secure query validation when creating new folders.
- Query documents by user ownership and share table entries referencing the user's id.

## 4. Agreed Feature Requirements
- 

## 5. Next Steps
- 

---

> **Note:** This report is intentionally left empty until after the SQL schema discussion takes place.