# Weekly Report

**Date:** 2026-05-18
**Branch:** dev
**Project:** Web-Project

---

## 1. Meetings

- **Date / Time:** 2026-05-18, lab session
- **Participants:** Full team + Professor
- **Key discussion points:**
  - Final pre-presentation review — walked through all completed features against the original requirements checklist
  - Professor confirmed the presentation format: live demo on the day, all team members present, each person speaks to their own section
  - Multi-device access demonstrated live in the lab — second laptop connected over the classroom Wi-Fi, registered a second user, and showed document sharing end-to-end
- **Decisions made:**
  - Presentation walkthrough order finalised: Authentication → Password Reset → Upload + Folders → Multi-Device + Sharing → AI Search
  - Each demo step will reference its key files so the professor can follow the code path during the presentation
  - No new features to be added — branch focus shifts to stability and documentation only

---

## 2. Progress

- **Completed tasks:**
  - Configured server to bind to `0.0.0.0:8000` for LAN access — confirmed working across two devices on the same network
  - Demonstrated live document sharing: User A uploaded a file on the host laptop, shared it by username to User B, User B opened `http://192.168.1.x:8000` on the second device and retrieved it from the **Shared with Me** tab
  - Verified `isSameOriginRequest()` in `auth/auth.php` continues to pass correctly when the host is a LAN IP rather than localhost
  - Completed presentation walkthrough document in `.docs/presentation_summary.md` — all five steps written with sub-sections, talk points, and key file tables
  - Added Mermaid architecture diagram (system overview `flowchart LR`) and AI search sequence diagram (`sequenceDiagram`) to presentation summary
  - All doc screenshots consolidated into `.docs/media/`; `dms.jpg` hero image retained at project root

- **Tasks in progress:**
  - Final dry-run of full demo end-to-end before presentation date
  - Merging `dev` → `main` once dry-run passes

---

## 3. Issues

- **Issues encountered:**
  - Windows Firewall blocked inbound connections to port 8000 on first attempt — the second device could not reach the app
  - PHP server defaulted to `localhost` binding, making it unreachable from any other device even on the same network

- **Resolution steps:**
  - Added a Windows Firewall inbound rule to allow TCP port 8000 for `php.exe`
  - Restarted server with `php -S 0.0.0.0:8000` — binds to all network interfaces, second device connected immediately

---

## 4. Decisions and Plan Changes

- Feature freeze in effect — no new functionality to be merged before the presentation
- `seed_demo.php` will be run at the start of the demo to set up the folder tree; confirmed it wipes and re-seeds cleanly so the demo state is always consistent
- `CLAUDE_API_KEY` to be set in the environment on the presentation machine before the session to ensure AI search reranking is live during the demo

---

## 5. Plan for Next Week

- **Planned tasks:**
  - Full end-to-end dry-run: register → upload → share → folder search → AI search → multi-device
  - Merge `dev` → `main`, tag release
  - Ensure `config/mail.php` and `.env` credentials are confirmed working on the presentation machine

- **Planned meetings:**
  - Presentation to professor — final submission
